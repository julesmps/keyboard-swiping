
cc_test(
  name = "trie-test",
  srcs = ["trie_test.cpp"],
  deps = [
    "//:trie",
    "@googletest//:gtest_main",
  ],
)

cc_test(
  name = "swipe_prediction-test",
  srcs = ["swipe_prediction_test.cpp"],
  deps = [
    "//:swipe-prediction",
    "@googletest//:gtest_main",
  ],
)
